# SESSION 2026-08-19 · Part 1

## Objective
1. Diagnose "website .bat won't launch" and fix it.
2. Review `SSIS-AIO.bat` for problems.
3. Create a master compliance rule book (`RULEBOOK.md`, ROOT) merging the two
   `Scratch\*PROMPT PRODUCTION GRADE.md` files verbatim, make it universal
   (no project-specific facts baked in), and run its own §0.0 activation
   check against this project.

## Env facts
No change from `BOOTSTRAP.md`'s 2026-08-18 verified table — not re-verified
this session (§C.2, nothing depended on a version change).

## Decisions
- **`start-website.bat` restored to ROOT** — supersedes the 2026-08-18 note
  "No more `.bat` files at root — moved into `Scratch\SATYAM-SCHOOL\`". User
  chose (via bundled question) to fix only `start-website.bat` back to ROOT;
  the other 4 (`start-student-app.bat`, `start-teacher-app.bat`,
  `start-attendance-app.bat`, `SSIS-AIO.bat`) stay in
  `Scratch\SATYAM-SCHOOL\`. Root copy content verified byte-identical to the
  last git commit (`git diff` empty).
- **`RULEBOOK.md` created at ROOT** — verbatim merge of both source files
  (diff-verified word-for-word against `Scratch\AI PROJECT PROMPT PRODUCTION
  GRADE.md` and `Scratch\GOVERNANCE SECURITY-PROMPT PRODUCTION GRADE.md`;
  only differences: header/`Source:` framing added, 6 curly→straight quote
  normalizations, whitespace) plus a new §0 enforcement layer (compliance +
  realignment protocol) and a §0.0 activation clause.
- **Made `RULEBOOK.md` fully generic** — removed the two spots that named
  this project ("SATYAM-SCHOOL"), per the file's own §H.5/§H.12 rule against
  embedding project facts into the rule file itself. Project facts stay in
  `BOOTSTRAP.md` only.
- **Both original source files kept** in `Scratch\` (not deleted) — user
  decision: they remain the untouched master templates for future projects;
  `RULEBOOK.md` is the active, project-attached instance.
- An earlier attempt to also strip literal "prompt" wording from
  `RULEBOOK.md` was made, then explicitly reverted by the user before the
  universalization pass — the file is back to the full "verbatim/Source"
  framing, now also genericized.

## Work done (file-by-file)
- `start-website.bat` (ROOT) — recreated, content matches `git show
  HEAD:start-website.bat` exactly (confirmed via `git diff` = empty).
- `RULEBOOK.md` (ROOT, new, untracked) — created, edited, reverted, then
  re-edited for universality. Current state: full verbatim PART I + PART II
  + §0 enforcement + §0.0 activation, no project-specific facts.
- `ai-context\SESSION-2026-08-19-1.md` (this file) — new.
- `work-log\LOG-2026-08-19.md` — new (plain-English mirror).
- `ai-context\BOOTSTRAP.md` — refreshed: "Root folder layout" corrected
  (start-website.bat back at ROOT), "Last checkpoint" updated.

## §0.0 activation-check result (ran against THIS project)
Mode: **ALREADY RUNNING** (unchanged from 2026-08-18 detection).
Scaffold check against PART I §0B "Definition of done" — all items already
present from the 2026-08-18 session:
- `AGENTS.md` (ROOT): §A/§D/§F/§H/§J0/§J/V5 verbatim + project section ✓
- `Scratch\SATYAM-SCHOOL\` all 7 subfolders present (`planning`, `coding`
  +`assets`, `debugging`, `suggestions`, `documentation`, `ai-context`
  +`archive`, `work-log`) ✓
- `planning\`: PLAN.md, DB-DESIGN.md, IMPL-SPEC.md, UI-SPEC.md, TODO.md,
  SECURITY-THREAT-MODEL.md, RELEASE-PLAN.md, GOVERNANCE-AUDIT.md ✓ (no
  DISCOVERY.md — not required; product is established, not a vague idea,
  §C0 doesn't apply)
- `documentation\SETUP-GUIDE.md` ✓
- `ai-context\BOOTSTRAP.md` ✓ (existed already — the "still missing
  bootstrap.md" assumption from earlier in this session was wrong; it was
  already there)
- Session + work-log files existed for 2026-08-18; this session adds the
  2026-08-19 ones.
- `Scratch\` git-ignored ✓ (`.gitignore:6 /Scratch/`), production code
  untouched, nothing committed (§A.9).
**Nothing was missing to create except today's own log entries** (this
file + the work-log entry), which are the automatic §D output for today's
changes.

## Blockers
None.

## Next steps
1. Unchanged from 2026-08-18 `BOOTSTRAP.md`: REQ-SEC-001..004 each still need
   a written, approved plan before any code; REQ-BUG-001..009 still need
   user prioritization.
2. User has not yet said whether to also fix the other 4 `.bat` files or
   leave them in `Scratch\SATYAM-SCHOOL\` long-term.

## Part 2 (later, same session) — git-ignore policy split + BOOTSTRAP.md moved to ROOT

### Decisions
- User asked to stage `RULEBOOK.md` + the log files; found the 3 continuity
  files (`ai-context\`/`work-log\`) were git-ignored by `RULEBOOK.md`'s
  default. User then said to include them, explicitly reasoning
  "if I lost it locally I should be able to recover them."
- Before un-ignoring, scanned `ai-context\`/`work-log\` for secrets — found
  a real one: `BOOTSTRAP.md` documented the live default `app_password`
  value in plaintext. Redacted it (exact value stays only in
  `planning\SECURITY-THREAT-MODEL.md`, which stays git-ignored) before
  tracking anything.
- Added `RULEBOOK.md` §0 item 8 (git-ignore exceptions are opt-in, scoped,
  audited). User then asked: shouldn't `AGENTS.md`/`BOOTSTRAP.md`/
  `RULEBOOK.md` just be tracked automatically? Correct — `AGENTS.md`/
  `RULEBOOK.md` already were (ROOT, never covered by the `Scratch/` ignore
  rule). Split §0 item 8 into **§0.8** (mandatory default: the continuity
  trio, incl. `BOOTSTRAP.md`) and **§0.9** (opt-in: `SESSION-*.md`/
  `LOG-*.md`/rest of `Scratch\`).
- User then asked to physically move `BOOTSTRAP.md` to ROOT, "so you dont
  need add extra rules for scratch" — this makes §0.8 trivially true for
  `BOOTSTRAP.md` (no gitignore exception needed for it at all, same as
  `AGENTS.md`/`RULEBOOK.md`) and simplifies the model. Done via `git mv`.

### Work done (file-by-file)
- `RULEBOOK.md` — §0 item 8 split into §0.8 (mandatory: continuity trio)
  + §0.9 (opt-in: rest of `Scratch\`). Re-verified PART I/PART II still
  100% verbatim against both source files after the edit (same cosmetic-only
  diff as every prior check — nothing in the verbatim body touched).
- `BOOTSTRAP.md` — moved ROOT-ward via `git mv` from
  `Scratch\SATYAM-SCHOOL\ai-context\BOOTSTRAP.md`. Added a "Location note"
  explaining the ROOT placement + links to `ai-context\`/`work-log\`.
  Updated its own git-ignore-exception note to reflect that it no longer
  needs the exception itself (only `ai-context\`/`work-log\` still do, for
  the `SESSION-*.md`/`LOG-*.md` files). Updated "Root folder layout" to
  list itself at ROOT.
- `AGENTS.md` — added a "Continuity files" list at the top of the
  project-specific section (AGENTS.md → RULEBOOK.md → BOOTSTRAP.md →
  ai-context\ → work-log\, in read order) and fixed 3 stale
  `Scratch\SATYAM-SCHOOL\ai-context\BOOTSTRAP.md` path references (root
  layout note, prep-workspace line, quick links) to point at
  `BOOTSTRAP.md` (root).
- `documentation\SETUP-GUIDE.md`, `planning\TODO.md` — fixed 3 stale
  `ai-context\BOOTSTRAP.md` pointer references to `BOOTSTRAP.md` (root).
  `Scratch\SATYAM-SCHOOL\ai-context\SESSION-2026-08-18-1.md` was
  deliberately left untouched — it's a historical record of what was true
  when it was written (yesterday, before the move existed) and should not
  be rewritten to match today's state.

### Verification
- `git status --short -- BOOTSTRAP.md "Scratch/SATYAM-SCHOOL/ai-context/BOOTSTRAP.md"`
  confirms the move (single `AM BOOTSTRAP.md` line, old path gone).
- `grep -rln "ai-context.BOOTSTRAP"` swept the whole repo; every remaining
  hit is either (a) generic PART I/PART II verbatim policy text describing
  the *default* convention (must not be edited — breaks the verbatim
  guarantee), (b) the intentional old-vs-new contrast notes in
  `BOOTSTRAP.md`'s own header, or (c) untouched historical logs. No stale
  "this is where it currently lives" claims remain in living reference docs.

### Blockers
None.

### Next steps
Unchanged from Part 1 above — REQ-SEC-001..004 and REQ-BUG-001..009 still
await the user's plan/prioritization; the 4 remaining `.bat` files'
long-term location is still undecided.

## Part 3 (later, same session) — continuity files moved to a dedicated `governance\` folder

### Decision
User asked for something cleaner than the Part 2 gitignore carve-out:
"instead of git scratch folder make new folder where agents.md
rulebook,bootstrap, ai context, work log remains without any sensitive
information and git them." Confirmed with the user first whether `AGENTS.md`
should move too — kept it at ROOT (recommended: AI tooling auto-discovers
`AGENTS.md` specifically at repo root; moving it risks it not being
auto-loaded). Everything else — `RULEBOOK.md`, `BOOTSTRAP.md`, `ai-context\`,
`work-log\` — moved into a new `governance\` folder at ROOT, sibling of
`Scratch\`, not inside it.

### Work done
- Re-scanned everything about to move for secrets (key/token/password/secret
  patterns + the specific known default-password string) — clean; the one
  real exposure found earlier (Part 2) was already redacted.
- `mkdir governance`; `git mv RULEBOOK.md governance/RULEBOOK.md`;
  `git mv BOOTSTRAP.md governance/BOOTSTRAP.md`;
  `git mv Scratch/SATYAM-SCHOOL/ai-context governance/ai-context`;
  `git mv Scratch/SATYAM-SCHOOL/work-log governance/work-log`. Verified
  `archive\` (empty) and the discovery `.txt` moved with `ai-context\`.
- `.gitignore` simplified back to a single `/Scratch/` line — the Part 2
  carve-out rules (`!/Scratch/SATYAM-SCHOOL/ai-context/` etc.) are now dead
  code since nothing tracked lives under `Scratch\` anymore; removed them.
- `RULEBOOK.md` §0 items 8/9 rewritten: §0.8 now describes the dedicated
  tracked-folder pattern (recommended name `governance\`) as the default,
  superseding PART I §B's literal "ai-context\/work-log\ nested inside
  Scratch\<ProjectName>\" default; §0.9 is now purely the secret-scan +
  verify + record procedure, no longer framed as a gitignore-carve-out
  exception. Re-verified PART I/PART II still 100% verbatim after the edit
  (identical diff to every prior check).
- `BOOTSTRAP.md`, `AGENTS.md` — every path reference to the old locations
  (root-level `BOOTSTRAP.md`, `Scratch\SATYAM-SCHOOL\ai-context\`/`work-log\`)
  updated to `governance\...`. `AGENTS.md`'s "Continuity files" list and
  "Quick links" rewritten accordingly.
- `Scratch\SATYAM-SCHOOL\documentation\SETUP-GUIDE.md`,
  `Scratch\SATYAM-SCHOOL\planning\TODO.md` — the 3 pointer references fixed
  again (they said "BOOTSTRAP.md, root" after Part 2; now say
  `governance\BOOTSTRAP.md`).

### Verification
- `ls` confirms `governance\` holds `RULEBOOK.md`, `BOOTSTRAP.md`,
  `ai-context\` (incl. `archive\` + the discovery `.txt`), `work-log\`; old
  `Scratch\SATYAM-SCHOOL\ai-context\`/`work-log\` no longer exist.
- `git status --short` shows clean `AM`/`A` entries under `governance/`,
  no leftover entries at the old paths.
- PART I/PART II verbatim-diff re-checked after every RULEBOOK.md edit this
  session — always the same 8+2 cosmetic-only hunks, never more.

### Blockers
None.

### Next steps
Unchanged — REQ-SEC-001..004, REQ-BUG-001..009, and the 4 remaining `.bat`
files' location are all still open, unrelated to this restructuring.

## Part 4 (later still, same session) — planning\ + documentation\ moved into governance\ too

### Decision
User asked to also add `TODO.md` and "necessary project requirements
files" into git. Asked one bundled clarifying question on scope (all of
`planning\`+`documentation\` vs. everything-except-SECURITY-THREAT-MODEL
vs. just TODO+PLAN) — user chose the full set, redaction included.

### Work done
- Full secret scan (`grep` for key/token/password/secret patterns +
  `Satyam@123` specifically) across `planning\` + `documentation\` before
  moving anything. Found **two** real exposures this round:
  `planning\DB-DESIGN.md` and `planning\SECURITY-THREAT-MODEL.md` both
  quoted the live default-password value in their write-ups. Redacted
  both (finding stays documented, value doesn't) — noted that the value
  already exists in git history via `mobile-app/SUPABASE_APP_AUTH.sql`
  (production code, unrelated to this doc reorganization), so redacting
  the docs is good hygiene but doesn't remove a pre-existing exposure.
  Confirmed no other secret patterns anywhere in either folder (the AWS/
  Supabase-key mentions found were all variable *names*, never values —
  `RELEASE-PLAN.md`, `SETUP-GUIDE.md` explicitly note values are masked/
  env-var-only).
- `mv Scratch/SATYAM-SCHOOL/planning governance/planning` and
  `mv Scratch/SATYAM-SCHOOL/documentation governance/documentation`
  (plain `mv`, not `git mv` — git refused "source directory is empty" for
  an untracked/ignored dir; `git add` after the move instead).
- Updated `governance\BOOTSTRAP.md`: "Continuity folder" section rewritten
  to cover this second move + the two new redactions; "Root folder layout"
  and "Mode" sections updated to list `planning\`/`documentation\` inside
  `governance\`; the "Architecture drift" table row's redaction note fixed
  (previously pointed at "local-only planning\SECURITY-THREAT-MODEL.md for
  the exact value" — no longer true now that file is redacted+tracked too;
  now correctly points at the SQL file).
- Updated `AGENTS.md`: "Continuity files" list extended with `planning\`
  and `documentation\SETUP-GUIDE.md` entries; "Scratch\SATYAM-SCHOOL\ now
  holds only..." line corrected (planning/documentation removed from the
  disposable-material description); 3 bare `planning\...` path references
  fixed to `governance\planning\...`.

### Verification
- `grep -rn "Satyam@123"` across `governance\` after the moves: zero
  matches.
- PART I/PART II re-diffed against both source files one more time —
  identical to every prior check this session (same 8+2 cosmetic-only
  hunks).
- Swept for stale `Scratch...planning`/`Scratch...documentation`
  references across `AGENTS.md` + all of `governance\*.md` — every
  remaining hit is inside PART I's own verbatim body (generic default
  text, must not be touched) or this session's own historical narrative.

### Blockers
None.

### Next steps
Unchanged — REQ-SEC-001..004, REQ-BUG-001..009, and the 4 remaining `.bat`
files' location are all still open.

## Part 5 (final, same session) — checked whether Scratch\ could be deleted; it couldn't (yet)

### Decision
User asked: "if everything removed from scratch then delete that folder."
Checked first rather than assuming — `Scratch\` is NOT empty:
`Scratch\` root still holds the 2 reusable prompt templates (kept per an
earlier explicit "keep both" decision); `Scratch\SATYAM-SCHOOL\` still
holds 4 working `.bat` launchers + 3 empty stub folders
(`coding\`/`debugging\`/`suggestions\`). Did NOT delete the folder —
reported the real contents instead of deleting on a false premise (§J2/§J3:
inspect before destructive action; never destroy without explicit,
informed approval). Found one genuinely stale item along the way: a
leftover duplicate `Scratch\SATYAM-SCHOOL\start-website.bat` from earlier
troubleshooting this session, redundant now that ROOT has the real one.
Asked; user confirmed delete — removed via `rm`.

### Work done
- Deleted the stale duplicate `start-website.bat` from
  `Scratch\SATYAM-SCHOOL\`.
- `RULEBOOK.md` §0.8 rewritten: previously listed `planning\`/
  `documentation\` as things that stay inside `Scratch/` by default — now
  stale after Part 4's move. Fixed to describe the "minimum vs. widened"
  scope explicitly (continuity trio + logs = minimum; planning\/
  documentation\ = optional widening, same secret-scan/approval
  requirement) and to only list the genuinely ephemeral material
  (`coding\` stubs, `debugging\`, `suggestions\`) as the Scratch/ default.
  Also added a note that reusable prompt/rule-book templates may live
  directly under `Scratch/` (not a project subfolder) and stay git-ignored
  there — matching this project's actual layout. Re-verified PART I/PART II
  still 100% verbatim (same diff as every check this session).
- `AGENTS.md` + `governance\BOOTSTRAP.md`: the "Scratch now holds only
  disposable/ephemeral material" lines were technically incomplete (didn't
  mention the 4 `.bat` launchers or the 2 templates still there) — rewrote
  both for full accuracy against the real, current filesystem state.

### Verification
- `find Scratch -maxdepth 2` and `find governance -maxdepth 2` both run
  and matched against every doc claim before editing.
- PART I/PART II re-diffed one final time — identical to every check this
  session.

### Blockers
None.

### Next steps
Unchanged — REQ-SEC-001..004, REQ-BUG-001..009 still need the user's
plan/prioritization. The 4 remaining `.bat` files' long-term location
(stay in `Scratch\SATYAM-SCHOOL\` vs. move somewhere permanent) is still
an open question — not resolved by this session, since they're functional
scripts, not prep material, and don't fit neatly into either `Scratch\`'s
"disposable" purpose or `governance\`'s "continuity record" purpose.

## Part 6 (final, same session) — refdocs\ moved into Scratch\, .gitignore simplified to one rule

### Decision
User: "move refdocs to scratch then and keep entire scratch git ignored" —
consolidate the last standalone root-level ignored folder into `Scratch\`
so one `/Scratch/` rule covers everything local-only instead of two
separate rules (`/Scratch/` + `/refdocs/`).

### Work done
- Verified `refdocs\` was already fully git-ignored/untracked before
  touching it (`git check-ignore -v` + `git status`) — plain filesystem
  `mv refdocs Scratch/refdocs`, no tracking implications either way.
- `.gitignore` — removed the separate `/refdocs/` rule; single `/Scratch/`
  rule now covers `Scratch\` root (2 prompt templates + `refdocs\`) and
  `Scratch\SATYAM-SCHOOL\` (disposable material + 4 `.bat` launchers).
- Fixed every `refdocs/...` path reference across living docs to
  `Scratch/refdocs/...`: `AGENTS.md` (1), `governance\BOOTSTRAP.md` (2
  living + re-wrote the "Continuity folder" section with a new paragraph
  for this move), `governance\documentation\SETUP-GUIDE.md` (1),
  `governance\planning\PLAN.md` (2, plus the whole "File map" tree
  diagram — which predated today's entire `governance\` reorg and was
  substantially stale — rewritten to match the current real tree),
  `governance\planning\RELEASE-PLAN.md` (2).
- `governance\planning\TODO.md` REQ-HYG-004 — this closed item's own
  2026-08-18 recommendation ("move refdocs out of the repo or add an
  explicit ignore rule") is exactly what this move accomplished — added a
  "Resolved 2026-08-19" note rather than leaving it read like an open
  suggestion.
- Left untouched (correctly): `governance\ai-context\SESSION-2026-08-18-1.md`
  and `governance\work-log\LOG-2026-08-18.md`'s `refdocs\` mentions —
  historical record of yesterday's earlier refdocs-related work (moving it
  out of root git tracking in the first place), accurate for when written.

### Verification
- `git check-ignore -v` on `Scratch/refdocs/env.png` and
  `Scratch/refdocs/vercel production.png` — both correctly matched by the
  single `/Scratch/` rule.
- `ls refdocs` at ROOT — confirms old path gone.
- Full repo sweep for bare `` `refdocs `` mentions (not prefixed
  `Scratch/`) — every remaining hit is either my own accurately-worded
  migration note or an untouched historical record.

### Blockers
None.

### Next steps
Unchanged — REQ-SEC-001..004, REQ-BUG-001..009 need the user's plan/
prioritization; the 4 remaining `.bat` files' long-term location is still
open.

## Part 7 (final, same session) — PROJECT_CONTEXT.md moved into governance\documentation\

### Decision
User asked why `PROJECT_CONTEXT.md` sat at ROOT when `TODO.md`/`ai-context\`
already exist, and whether it should move into `ai-context\` if still
needed. Read the file first (435 lines: route map, per-module build
status, DB schema, shared utilities, constraints, file-size reference, git
history) before answering — it's a pre-existing, human-authored technical
architecture reference, a genuinely different genre from `TODO.md` (task
checklist) and `ai-context\` (AI session delta logs). Recommended
`governance\documentation\` instead (same genre as `SETUP-GUIDE.md`, both
"living reference docs") — user agreed, asked to move it there.

### Work done
- `git mv PROJECT_CONTEXT.md governance/documentation/PROJECT_CONTEXT.md`
  — clean rename, git detected it as `R` (100% similarity), so history is
  preserved, not a delete+re-add.
- Fixed every cross-reference across the repo: `AGENTS.md` (4 spots,
  including adding it as item 8 in the "Continuity files" list),
  `governance\BOOTSTRAP.md` (7 spots, including a new paragraph in the
  "Continuity folder" section explaining the move), `governance\planning\
  {DB-DESIGN,PLAN,SECURITY-THREAT-MODEL,TODO,UI-SPEC}.md` (11 spots total,
  including the "File map" tree diagram). `file:line` citations (e.g.
  `PROJECT_CONTEXT.md:67`) kept their line numbers — moving a file doesn't
  change its content, so old citations remain accurate, just re-prefixed
  with the new path.
- Left untouched (correctly): `SESSION-2026-08-18-1.md`/`LOG-2026-08-18.md`
  historical mentions of `PROJECT_CONTEXT.md` staying at root — accurate
  for when written.

### Verification
- `git status --short` shows `R  PROJECT_CONTEXT.md ->
  governance/documentation/PROJECT_CONTEXT.md`.
- Repo-wide sweep for bare (unprefixed) `PROJECT_CONTEXT.md` mentions —
  every remaining hit is either historical record or my own prose
  correctly describing the move itself.

### Blockers
None.

### Next steps
Unchanged — REQ-SEC-001..004, REQ-BUG-001..009 need the user's plan/
prioritization; the 4 remaining `.bat` files' long-term location is still
open.

## Part 8 (final, same session) — RULEBOOK.md §0.8/§0.0 caught up to today's actual structural changes

### Decision
User: "update rule book accordingly there was massive structural changes
we have done" — after Parts 1-7 (gitignore carve-out → dedicated
`governance\` folder → widened to `planning\`/`documentation\` →
`refdocs\` consolidated into `Scratch\` → `PROJECT_CONTEXT.md` moved in),
`RULEBOOK.md` §0.8 already described the folder pattern but had two real
gaps left from the later rounds.

### Work done
- §0.8 — added: (a) pre-existing project reference docs (the
  `PROJECT_CONTEXT.md` case) as an explicit `documentation\` widening
  category, with the reasoning for why it's a different genre from
  `ai-context\`/`TODO.md`; (b) the "consolidate local-only material under
  one `/Scratch/` rule instead of several" principle (the `refdocs\` case)
  as a generalizable hygiene note.
- §0.0 activation clause, step (b) — this was the real gap: it still told
  a fresh agent to build PART I §0B's literal default (BOOTSTRAP/SESSION/
  LOG nested inside `Scratch\<ProjectName>\`) rather than §0.8's actual
  recommended pattern. A brand-new project activating today would have
  built the OLD structure and needed the same migration this project just
  went through. Rewrote it to build the §0.8 dedicated-folder pattern
  directly from first activation.
- Re-verified PART I/PART II 100% verbatim (md5sum of the diff against
  both source files matches every prior check exactly — zero drift across
  8 parts of edits today).

### Verification
`diff` against both `Scratch\*PROMPT PRODUCTION GRADE.md` source files,
piped to `md5sum`, produces the identical hash to the very first
verbatim-check done at the start of this session.

### Blockers
None.

### Next steps
Unchanged — REQ-SEC-001..004, REQ-BUG-001..009 need the user's plan/
prioritization; the 4 remaining `.bat` files' long-term location is still
open. Today's restructuring work (Parts 1-8) is complete and internally
consistent as of this entry.

## Part 9 (final, same session) — added an actual workspace-structure diagram to §0.8

### Decision
User: "workspace structure is not updated" — correct catch: §0.8 had
prose describing the `governance\` pattern but no visual ASCII tree, and
PART I §B's own tree diagram (verbatim, must not be edited) still shows
the old default. §0.0(b) already cross-referenced §0.8 as superseding §B,
but nothing showed the actual current shape at a glance.

### Work done
- Added a labeled ASCII tree ("§0.8 workspace structure — supersedes §B's
  diagram below") directly inside §0.8, after its prose paragraph, before
  §0.9. Matches the real structure this project now has.
- Checked whether `AGENTS.md` needed the same fix — it doesn't carry §B
  at all (only §A/§D/§F/§H/§J0/§J/V5), so no change needed there.
- Re-verified PART I/PART II 100% verbatim — checksum matches every prior
  check exactly.

### Blockers
None.

### Next steps
Unchanged.

## Part 10 (final, same session) — §B and the master template itself updated: new structure baked in for every future project

### Decision
User checked §B again after Part 9 and said it "is not updated" — correct:
§B is PART I's verbatim body, deliberately left untouched through Parts
1-9 to preserve the word-for-word match against the source file. Asked
directly whether to break that verbatim guarantee and edit §B; user:
"i want this new structure for every project onward so update it" — an
explicit decision to make the `governance\` pattern the actual new
default, not just an amendment layered on top, and to apply it to the
**master template** (`Scratch\AI PROJECT PROMPT PRODUCTION GRADE.md`) so
future projects inherit it directly, not just this project's copy.

### Work done
- Edited the master template directly (not RULEBOOK.md first) — found the
  old `Scratch\<ProjectName>\ai-context\`/`work-log\`/`planning\`/
  `documentation\` default referenced in far more places than just §B:
  the intro blockquote, §0 item 1, §0B's "Definition of done" checklist,
  §A.2 (a CORE DIRECTIVE — was factually wrong: claimed all of `Scratch/`
  is excluded from git, no longer true), §B (prose + diagram), §C's
  blanket "paths relative to" statement + its numbered list, §D's blanket
  statement + `BOOTSTRAP.md` path, §H.12, §I.2. Fixed all of them
  consistently — `ai-context\`/`work-log\`/`planning\`/`documentation\`
  now relative to `governance\`; `coding\`/`debugging\`/`suggestions\`
  stay relative to `Scratch\<ProjectName>\`; `BOOTSTRAP.md` sits directly
  in `governance\`, not nested under `ai-context\`.
- Spliced the entire updated master content into `RULEBOOK.md`'s PART I
  (replacing it wholesale via `sed`, not hand-editing — lower risk of
  drift than 15+ separate manual edits), keeping only the header/Source
  framing and trailing `---` separator RULEBOOK.md adds on top. Verified
  byte-for-byte identical to the master template afterward.
- Updated the top intro blockquote: dropped the implication that PART I
  matches "the original historical text" and replaced it with "kept in
  exact sync with its master template file" — the real invariant now.
- Consolidated §0 items 8-9 (which existed specifically to work around
  PART I §B's old default) into one short item: a pointer to §B (avoid
  duplicating its now-correct content) plus migration guidance for any
  project still running the pre-2026-08-19 pattern. Removed the
  now-redundant duplicate ASCII diagram from §0 (§B has the authoritative
  one).
- Synced `AGENTS.md`'s §A/§D/§H (the sections required to be verbatim
  copies per §I.5) — found and fixed the same stale paths there
  independently (3 spots: §A.2's prose, §D's `BOOTSTRAP.md` path +
  blanket statement, §H.12's parenthetical). Diffed every AGENTS.md
  section (§A/§D/§F/§H) against RULEBOOK.md's PART I section-by-section
  after fixing — all now byte-for-byte identical (aside from a
  pre-existing, unrelated curly-vs-straight-quote cosmetic difference that
  predates this session and wasn't introduced by this work).

### Verification
- `diff` of PART I against the master template: only the intentional
  header/separator lines differ — content identical.
- `diff` of PART II against its unmodified source: identical to every
  check this session — untouched.
- `diff` of AGENTS.md's §A, §D, §F, §H against RULEBOOK.md's matching
  PART I sections: empty (after the 3 AGENTS.md-specific fixes).
- `grep` sweep of the master template for the old nested-path pattern:
  zero remaining matches.

### Blockers
None.

### Next steps
Unchanged — REQ-SEC-001..004, REQ-BUG-001..009 need the user's plan/
prioritization; the 4 remaining `.bat` files' long-term location is still
open. This is the final structural-consistency pass for today — §B, the
master template, RULEBOOK.md, and AGENTS.md are now all in agreement.

## Part 11 (session close) — two audit passes + final BOOTSTRAP refresh

### Work done
- User ran two verification-only requests (no file changes needed):
  1. "recheck rulebook for universality rules" — grepped for every
     project-identifying term (SATYAM, admin-panel, mobile-app, Next.js,
     Flutter, Supabase, Vercel, etc.) across `RULEBOOK.md`: zero matches.
     Confirmed §H.5/§H.12 self-referential universality rules intact and
     repointed correctly. Confirmed PART II's `{PLACEHOLDER}` block is
     still unfilled (stayed generic). Confirmed no stale `§0.9`
     cross-references after the earlier consolidation. Noted one
     intentional non-issue: `governance\` lost its earlier "may call it
     something else" flexibility when folded into §B — correct, since §B
     already says "Keep exact names" for every other folder; consistent,
     not a regression.
  2. "folder structure now new in whole rulebook or still old structure
     reflects anywhere" — swept the entire file for the literal old
     nested pattern (`Scratch\<ProjectName>\ai-context\` etc.): zero
     matches. Checked bare `ai-context\`/`work-log\`/etc. mentions (still
     ~10-18 each, matching the original document's own style) all fall
     under a "relative to `governance\`" blanket statement. Checked PART
     II for structural assumptions: none — fully generic. The only
     surviving mention of the old pattern is the §0.8 migration note's
     own description of it as "the OLDER pattern" — correctly framed as
     historical/instructional, not a leftover.
- `governance\BOOTSTRAP.md` — found and fixed one real staleness during
  this pass: the "Location note" still described `governance\` as an
  *exception* to PART I's default ("not the PART I default... per
  RULEBOOK.md §0.8") — no longer accurate now that §B itself IS the
  default and §0.8 no longer contains that framing (consolidated in Part
  10). Fixed. Also rewrote "Last checkpoint" — it only summarized Part 1,
  not the full day (Parts 1-11).
- User then said "log and session end" — this entry + the work-log mirror
  are that close-out, per §D (mandatory, automatic logging) and §A.10/§F
  (session-end report requirement).

### Verification
No code, config, or governance-rule content changed in this part — pure
audit + one stale doc-note fix. `BOOTSTRAP.md`'s other sections (Mode,
Approvals, Environment, Root folder layout) already reflected the day's
final state from Part 10's edits — spot-checked, no further staleness
found.

### Blockers
None.

### Session-end summary (§A.10/§F)
- **Phase:** OPERATE (informal, retroactive — unchanged; this entire
  session was scaffold/governance work, not application code).
- **Approved this session:** the `governance\` folder restructuring
  itself (explicit, incremental approval at each step — gitignore
  exception → dedicated folder → widened scope → refdocs consolidation →
  PROJECT_CONTEXT.md move → baked into PART I §B as the new default for
  all future projects).
- **Pending:** REQ-SEC-001..004 (each needs its own written plan before
  any code), REQ-BUG-001..009 (need prioritization), the 4 remaining
  `.bat` files' long-term location (undecided).
- **Git:** everything staged (`.gitignore`, `AGENTS.md`, all of
  `governance\`, the `PROJECT_CONTEXT.md` rename), nothing committed —
  per §A.9, waiting on explicit instruction to commit.
- **Next trigger phrase:** none of today's work opened a phase gate — no
  trigger phrase is pending. If you want this committed, say so
  explicitly (§A.9 requires it).
