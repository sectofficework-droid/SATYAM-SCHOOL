# SESSION-2026-09-07-1 — Play Store publishing: store-listing assets + reviewer test accounts

**Governance gap flagged first (§0.3 realign, read-only — not backfilled this
session, out of scope for what was asked):** the Play Store publishing effort
covering release signing (`upload-keystore.jks`/`key.properties`,
`android/app/build.gradle.kts` wired to it, commit `394409b`) and the public
privacy-policy page (`https://satyam-school-privacy-policy.vercel.app/`) ran
in prior sessions (~2026-09-05/06) with **no `governance\` log entry at all**
— same undocumented-work pattern already on record for 2026-08-21→24 in
`BOOTSTRAP.md`. Not reconstructed here; flagging so a future session doesn't
assume it never happened. This session's own work (below) is the first of
this publishing effort actually logged in `governance\`.

## What this session did

1. **Store listing assets** (`mobile-app/`, untracked, not yet staged):
   - `store-listing-teacher.md` / `store-listing-student.md` — app name +
     short/full descriptions, written from the real route tables
     (`lib/app/routes/app_pages_{teacher,student}.dart`), not guessed.
   - `store-icon-512.png` — 512×512 hi-res icon, downscaled via `ffmpeg` from
     the existing iOS AppIcon source (same school-crest image already
     shipped in the app; not a new asset).
   - `feature-graphic-teacher.png` / `feature-graphic-student.png` —
     1024×500 Play feature graphics, hand-authored HTML/CSS rendered via
     headless Chrome (`chrome.exe --headless=new --screenshot=...`).
   - `store-screenshots/` — 8 on-device screenshots (4 per flavor), captured
     via BlueStacks (`adb connect 127.0.0.1:5555`) on **release** builds
     (debug builds show a Flutter RenderFlex-overflow debug banner on the
     Home screen — not representative, rebuilt as release specifically to
     avoid it).

2. **PII found in 3 of the 8 screenshots, fixed by redaction, not by faking
   data** — `student` home + fees screens showed a real enrolled child's
   name/roll/enrollment-no/photo and a real staff member's name in a payment
   record; `teacher` "My Students" showed a full real class roster (12 real
   children's names). User explicitly ruled out inserting dummy data for
   this. Fixed with `ffmpeg drawbox` (black-box over the specific
   name/photo/ID regions, computed by pixel-coordinate inspection of each
   900×1600 screenshot) — final images keep working avatars/roll numbers/UI
   chrome, only the identifying text/photos are covered. `-nostdin` is
   required on the `ffmpeg` invocation or it hangs waiting on stdin (hit
   this once, cost a 2-minute timeout). Un-redacted originals were deleted,
   not renamed — never landed in a tracked or shared location.

3. **App-access reviewer test accounts created directly in the production
   Supabase project** (`hxkowdaugkkumvzyfsai.supabase.co`), for Google
   Play's reviewer credentials requirement — the prior QA accounts
   (`ADMIN TESTSTUDENT` enrollment `0049` / `ADMIN TESTSTAFF` `EMP030`) were
   deleted 2026-08-28 (see `LOG-2026-08-28.md`), so new ones were needed:
   - Teacher: `employees` row, `emp_code = 'EMP900'`, name "Play Store
     Reviewer", set as `class_teacher_of_section_id` for the pre-existing,
     verified-empty **"ADMIN QA" class / Section A** (`class_id`
     `dcccd7ad-b98d-4b2e-ac2c-9469d74c2696`, `section_id`
     `ee248e6f-72c4-4fcc-9053-07ba09ef6127` — confirmed zero real students
     and no assigned class teacher before use; this looks like the same
     slot the old deleted QA accounts occupied).
   - Student: `students` + `student_enrollments` row, name "Play Store
     Reviewer", `grno = 'QA-9001'`, enrollment no `9001` (existing max was
     `471` — well clear of any real enrollment number), roll `1`, same
     section, placeholder `father_name`/`mother_name`/`mobile1` (`'N/A'` /
     `'0000000000'`) to satisfy NOT NULL constraints — no real family data
     used, per J0O.
   - Password hashed via `crypt(pw, gen_salt('bf'))`, same bcrypt scheme the
     real `student_login`/`teacher_login` RPCs already check against
     (post-REQ-SEC-001, see 2026-08-21 checkpoint) — not a new/weaker scheme.
   - **Verified via the actual RPCs**, not just a raw row check: called
     `select teacher_login('EMP900', <pw>)` and
     `select student_login('9001', <pw>)` directly, both returned the
     expected profile JSON. **Not yet verified end-to-end in the mobile app
     UI on a device** — the agent does not type passwords into login
     screens itself (standing rule); the user would need to do that manual
     check if UI-level confirmation is wanted.
   - Per §J6, **the real password is intentionally NOT recorded in this
     file, `BOOTSTRAP.md`, or any other governance file** — given to the
     user directly in-session (for pasting into Play Console's "App access"
     field) and separately in the agent's own private cross-session memory
     store (outside this repo, not part of `governance\`).

4. **Read-only finding, not fixed (relates to open REQ-SEC-002, corrects its
   figure) — Supabase's own security advisor** (`get_advisors`-equivalent,
   called incidentally while inspecting the schema for step 3) reports
   **73 tables** with RLS disabled, including `employees`, `fee_payments`,
   `student_documents`, `leave_requests`, and most of the admin-panel-only
   tables — a materially larger, now-authoritative count than the "~25
   tables" figure in `TODO.md`/`SECURITY-THREAT-MODEL.md` (which was derived
   from grepping `mobile-app/SUPABASE_*.sql` files, i.e. mobile-app-facing
   tables only, not the full live schema). `students` and `admin_users`
   remain correctly locked (matches the 2026-09-04 partial fix already on
   record). Not investigated further or acted on this session — flagged to
   the user in-chat, and to `TODO.md` (see below), per J14.

## Files changed
- New, untracked, not staged: `mobile-app/store-listing-teacher.md`,
  `mobile-app/store-listing-student.md`, `mobile-app/store-icon-512.png`,
  `mobile-app/feature-graphic-teacher.png`,
  `mobile-app/feature-graphic-student.png`, `mobile-app/store-screenshots/`
  (8 PNGs).
- Database (production, DML only, no schema change): 1 new row in
  `employees`, 1 new row in `students`, 1 new row in `student_enrollments`.
  No migration file — these are ordinary application rows, not schema, so
  no `SUPABASE_*.sql` file was needed (consistent with how `RULEBOOK.md` §J5
  distinguishes schema changes from data changes; this was reviewed as a
  data change, not a destructive or schema-altering one).
- No code changed. No commits made — git state unchanged from session start
  (`debiprasad`, same untracked files, same pre-existing
  `.claude/settings.local.json` modification noted in every recent
  checkpoint).

## Verified
- `ffprobe`-confirmed pixel dimensions before computing redaction boxes
  (900×1600 for all 3 flagged screenshots).
- Visually re-read every redacted PNG (`Read` tool) before accepting it;
  the teacher-roster first pass (full-row black bars) was rejected as
  unusable for a store listing and redone narrower (name-text only) before
  acceptance — two iterations, both visually verified.
- Reviewer accounts verified live against the real `teacher_login` /
  `student_login` RPCs (SQL-level), not just row existence.

## Not verified
- Reviewer accounts have not been exercised through the actual app UI on a
  device/emulator.
- No Play Console listing has been created yet; none of these assets have
  actually been uploaded anywhere.

## Next steps
1. Data Safety form + content rating questionnaire in Play Console (data
   collected: names, attendance/marks/fee records — explicitly NOT
   camera/face-scan, that only exists in the unpublished `attendance` kiosk
   flavor).
2. Recruit 12+ opted-in closed-testing testers — **confirmed this session
   this is a new Play Console developer account**, so both apps need
   12+ testers actually installing/opening the app across 14 consecutive
   days before Production unlocks for either. This is the long pole in the
   timeline; start it before the remaining prep work if the timeline
   matters.
3. Create both Play Console listings, upload the two signed AABs, set
   tracks (Teacher → Closed testing confirmed; Student → track still TBD
   per the existing open item in `BOOTSTRAP.md`/prior sessions).
4. REQ-SEC-002 now has a corrected, larger scope (73 tables per the live
   advisor, not ~25) — still nobody's fix-it task yet, needs the user's
   decision on priority same as before. Do NOT auto-apply
   `ENABLE ROW LEVEL SECURITY` without policies (would just break access) —
   noted by the advisory tool itself.
5. Governance-gap backfill for the 2026-09-05/06 signing + privacy-policy
   work is still outstanding (see flag at top of this file) — not scheduled,
   just flagged again so it isn't lost.
