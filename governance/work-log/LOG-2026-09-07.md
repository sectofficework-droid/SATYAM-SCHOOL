# LOG-2026-09-07 — Play Store listing assets + reviewer test accounts

## What I set out to do
Continue getting the Teacher App and Student App ready for the Play Store:
build the actual store listing (icon, banner, screenshots, descriptions),
and set up login credentials Google's reviewers can use to test both apps.

## What I did

**Store listing assets** — wrote the app descriptions for both apps (based
on what the apps actually do, not guesswork), made the 512×512 icon and the
1024×500 banner images, and took 8 real screenshots on both apps using
BlueStacks. All of this is sitting in `mobile-app/` right now, not uploaded
anywhere yet.

**Privacy fix on 3 screenshots** — 3 of the 8 screenshots I took showed real
information: a specific child's name/photo/roll number, a real staff
member's name on a payment record, and a full class list of real children's
names. You told me not to fake this with dummy data, so instead I blacked
out just the identifying names/photos on those 3 images directly, keeping
everything else (icons, numbers, layout) visible. The original un-redacted
screenshots were deleted, not kept anywhere.

**Reviewer test accounts** — Google requires you to give app reviewers a
working login. The old test accounts (enrollment `0049` / employee `EMP030`)
were deleted back on 2026-08-28, so I made two new ones, placed in an
already-empty "ADMIN QA" test class so they don't show up mixed in with any
real students or teachers:
- Teacher app login: employee ID `EMP900`
- Student app login: enrollment no `9001`
- (Password given to you directly in chat — not written into any project
  file, per the project's own rule about never putting real passwords in
  these logs.)

I double-checked both logins actually work by calling the same
login-checking code the app itself uses — both succeeded. I did not open the
actual app and type the password in myself (I don't do that, even for test
accounts) — if you want it double-checked on-screen, you'd need to log in
once on BlueStacks yourself.

**One extra thing I noticed, not fixed** — while looking at the database I
ran Supabase's built-in security checker and it flagged **73 tables**
(including fee records and employee records) where anyone holding the app's
public key could read or edit every row. This is a bigger version of a
security gap already on this project's TODO list (previously estimated at
about 25 tables) — I did not change anything, just flagging the corrected,
larger number for whenever you want to prioritize it.

## Current state
- 8 files/folders sitting ready in `mobile-app/`, not committed to git, not
  uploaded to Play Console yet.
- 2 reviewer accounts live in the real database, verified working.
- Nothing pushed, nothing deployed, nothing deleted from production data
  other than the intentional cleanup of un-redacted screenshot originals
  (which were never shared anywhere).

## Next steps
1. Fill out Google's Data Safety form + content rating questionnaire.
2. Start recruiting your 12+ test users now — confirmed this is a new
   developer account, so Google requires 12+ people actually using the app
   for 14 straight days before either app can go fully public. This takes
   real calendar time, so it's worth starting early.
3. Actually create the listings in Play Console and upload the two apps.
4. Decide when you want to tackle the bigger security-exposure item found
   today (73 tables, not urgent for the Play Store submission itself).
