# LOG — 2026-08-19

## What I set out to do
- Figure out why the website `.bat` launcher wasn't starting.
- Check `SSIS-AIO.bat` (the menu launcher) for bugs.
- Build one master rule book file (`RULEBOOK.md`) that combines the two
  long governance/workflow prompt documents, so any AI assistant working on
  this project follows all of both, automatically, every time.

## What I did
1. **Website `.bat` fix.** It wasn't broken — it had been moved into a notes
   folder (`Scratch\SATYAM-SCHOOL\`) instead of staying at the project root,
   so double-clicking it from the usual place found nothing. Restored a
   working copy to the project root (`start-website.bat`), verified it
   actually starts the dev server on `localhost:3000`.
2. **`SSIS-AIO.bat` review.** Tested the menu logic directly (the
   `if ... & goto menu` pattern is a known batch-file trap) — confirmed it
   works correctly here. Checked its dependencies (Flutter SDK, app entry
   files) — all present. No problems found.
3. **`RULEBOOK.md`.** Built one file containing every rule from both source
   documents (verified word-for-word by diffing), plus a new enforcement
   section that tells any AI: check every action against this book, and if
   you ever slip, stop and fix it before continuing. Then made it fully
   reusable — removed the two spots that mentioned this project by name, so
   the exact same file can be dropped into any other project unchanged and
   it will still work. Added a rule that says: the moment an AI finds this
   file in a project, it should check what required setup files exist and
   create whatever's missing, without being asked.
4. **Ran that check on this project.** Turned out almost everything was
   already set up from yesterday's session (rule book, plans, security
   notes, setup guide, current-state snapshot). Nothing needed creating
   except today's own log entries, which you're reading now.

## Decisions
- `start-website.bat` stays at the project root; the other 4 launcher
  `.bat` files stay in the `Scratch\SATYAM-SCHOOL\` notes folder for now.
- Keep both original prompt documents in the notes folder as untouched
  master copies, alongside the new `RULEBOOK.md`.

## Files changed
- `start-website.bat` (root) — restored.
- `RULEBOOK.md` (root) — new.
- `ai-context\SESSION-2026-08-19-1.md` — new (technical detail).
- `ai-context\BOOTSTRAP.md` — small correction to the "what's at the root"
  note.

## Current state
Nothing about the live website or apps changed. This was all documentation
and a launcher-script fix — no production code touched. Nothing committed
to git (staged files stay unstaged until you say to commit).

## Next steps
- Decide whether the other 4 `.bat` files should also move back to the
  root, or stay where they are.
- The security fix (REQ-SEC-001, password hashing) is still waiting on a
  written plan before any code gets touched, per yesterday's session.

## Later the same day — made the log files recoverable, moved BOOTSTRAP.md

You asked to also put `RULEBOOK.md` and the log files into git, in case
you ever lose the local copy. Before doing that I checked those files for
anything sensitive first — and found one real problem: the current-state
file (`BOOTSTRAP.md`) had the live default password for every user account
written out in plain text. Removed that value before turning git tracking
on (the underlying issue itself is unchanged — still on the to-fix list —
just not sitting in a file that's about to go into git history).

Then you pointed out that `AGENTS.md`, `BOOTSTRAP.md`, and `RULEBOOK.md`
should just always be saved in git automatically, without needing to ask
each time. Agreed, and updated the rule book to say so. You then asked to
physically move `BOOTSTRAP.md` out of the notes folder and up to the
project root, next to `AGENTS.md` and `RULEBOOK.md` — done. All three now
sit together at the root and link to each other and to the deeper
session/log folders, so anyone (human or AI) opening the project can find
the current state in one obvious place.

## Files changed (this later part)
- `BOOTSTRAP.md` — moved from the notes folder to the project root.
- `RULEBOOK.md`, `AGENTS.md` — updated to reflect the new file locations
  and cross-reference each other.
- `documentation\SETUP-GUIDE.md`, `planning\TODO.md` — two outdated path
  mentions corrected.

## Next steps (unchanged)
- Decide whether the other 4 `.bat` files should also move back to the
  root, or stay where they are.
- The security fix (REQ-SEC-001, password hashing) is still waiting on a
  written plan before any code gets touched.

## Even later the same day — one clean folder instead of a gitignore trick

You asked for something smoother than the earlier fix: instead of poking a
hole in the notes-folder's gitignore rule, put everything that should
survive a lost laptop into one proper folder, and make sure none of it
contains anything sensitive first.

Checked with you first whether `AGENTS.md` should move too — kept it at
the project root, since that's the one file AI tools specifically look
for there. Everything else (`RULEBOOK.md`, `BOOTSTRAP.md`, the session
history, the plain-English logs) now lives together in a new `governance\`
folder at the root. Re-checked all of it for secrets before moving —
nothing found. The notes folder (`Scratch\`) is back to being fully
excluded from git with no special-case rules in it — simpler and easier to
reason about.

## Files changed (this part)
- New `governance\` folder: `RULEBOOK.md`, `BOOTSTRAP.md`, `ai-context\`,
  `work-log\` all moved here.
- `.gitignore` simplified back to one line.
- `AGENTS.md`, `BOOTSTRAP.md`, and two reference docs updated so every
  link points to the new location.

## Next steps (still unchanged)
- Decide whether the other 4 `.bat` files should also move back to the
  root, or stay where they are.
- The security fix (REQ-SEC-001, password hashing) is still waiting on a
  written plan before any code gets touched.

## One more addition, same day — the requirement docs moved in too

You asked to also make `TODO.md` and the other project-requirement
documents recoverable the same way. Before moving anything, checked all
of them for sensitive content first — and found two more spots (in the
design doc and the security write-up) that quoted the same live default
password from earlier. Removed the value from both, same as before — the
underlying issue itself is unaffected, still on the fix list.

Worth knowing: that exact password value is already sitting in the
project's real database setup file, which has been committed to git for a
while now — so this cleanup doesn't erase an existing exposure, it just
stops the planning documents from repeating it.

`planning\` (the specs: scope, database design, implementation spec, UI
spec, task list, security write-up, release plan) and `documentation\`
(the setup guide) now live inside `governance\` too, next to everything
else. The notes folder (`Scratch\`) now only holds genuinely throwaway
material — coding drafts, bug notes, feature ideas.

## Last thing, same day — checked if the notes folder could finally be deleted

You asked: if everything's been moved out of the notes folder, delete it.
Checked first instead of assuming — it's not actually empty. It still
holds the two original prompt documents (which you asked to keep) and the
4 launcher `.bat` files that never moved. So I left the folder in place
and told you exactly what's still in it, rather than deleting something
real on a guess.

One thing genuinely was leftover junk though: an old duplicate copy of
`start-website.bat` sitting in the notes folder from earlier
troubleshooting, now pointless since the real one lives at the project
root. Asked, you said delete it — done.

Also cleaned up the rule book and current-state notes so they accurately
describe what's actually in each folder now, instead of an outdated
description from before today's reorganizing.

## Truly the last thing — the reference-images folder moved in too

You asked to move `refdocs\` (the dashboard screenshots, planning PDF,
logo/photo files) into the notes folder as well, and keep the whole notes
folder excluded from git with one simple rule instead of several. Done —
it was already never tracked, so this was just a tidy-up, no security
concern. The project's `.gitignore` is down to a single line covering
everything local-only now, instead of two separate rules. Also caught and
fixed one genuinely outdated file map (in the requirements doc) that still
showed yesterday's folder layout instead of today's.

## And finally — the technical reference doc moved too

You asked why `PROJECT_CONTEXT.md` was still sitting at the project root
when we already have a task list and session logs. Read it first before
answering — it's not the same kind of thing as either: 435 lines of real
architecture reference (routes, feature status, database schema), written
before any of today's reorganizing, not something an AI session log
replaces. Recommended putting it next to the setup guide instead of the
session-log folder, since it's the same kind of document. You agreed —
moved it there, and fixed every place in the project that pointed to its
old location, including the setup guide's own outdated map of the folder
structure.

## Wrapping up — the rule book itself now matches everything we actually did

You asked me to make sure the rule book reflects all the structural
changes from today, not just the individual project files. Checked it
against everything we actually did — found two real gaps. First, it
hadn't caught up to two of today's later moves (the technical-reference
doc, and consolidating the screenshots folder). Second, and more
important: the instructions for setting this pattern up on a brand-new
project still described the old, clunkier way of doing it — meaning a
future project starting fresh would have built today's starting point,
not today's ending point, and would eventually need the exact same
cleanup we just did. Fixed both, so the next project gets it right from
day one.

Double-checked one more time that the rule book's core content still
matches the two original source documents word-for-word — it does, with
zero drift across every change made today.

## One more fix — an actual picture of the new folder layout

You pointed out the workspace structure still wasn't updated — right catch.
The rule book had words describing the new folder layout, but nowhere did
it actually draw it out the way the original document draws its own
default layout. Added that picture in the same spot as the written
description, so anyone reading the rule book can see the real shape at a
glance instead of having to piece it together from prose.

## The real fix — making the new layout the actual default, everywhere

You checked again and said the structure section still wasn't updated —
and you were right to push on it. What I'd added before was a note next
to the original, unedited section, not a change to the section itself. I
asked directly: did you want me to actually rewrite that original section,
even though it would no longer match the master document word-for-word?
You said yes — you want this new layout to be what every future project
gets by default, not just something bolted on for this one.

So I went further than just this project's copy: edited the master
template document itself (the one this project's rule book was built
from), since that's what future projects will actually start from. Found
the old layout referenced in far more places than just the one section —
about a dozen spots across the whole document assumed the old nesting.
Fixed all of them consistently, then copied that updated master content
back into this project's rule book so the two stay identical, word for
word, going forward.

Also caught that `AGENTS.md` (the short version of the rule book that's
supposed to always match certain sections of the full one exactly) had
independently drifted stale in three of those same spots — fixed those
too, then checked every shared section between the two files line by line
to confirm they match.

## Two last double-checks, then closing out for the day

You asked me to double-check the rule book two more times: once to make
sure nothing project-specific had leaked into it, and once to make sure
the old folder layout wasn't still showing up anywhere in it. Both came
back clean — no project-specific mentions anywhere, and no trace of the
old layout left except in the one spot that's supposed to describe it (a
note explaining how to migrate away from it, for older projects). Along
the way I found and fixed one small leftover: the current-state file's
own note about its home folder still described it as a special exception
rather than just the normal default it now is.

## Session close

Here's where things stand at the end of today:

- **What changed:** this was entirely documentation and folder
  reorganization work — a rule book, a current-state file, session logs,
  and specs, all made properly recoverable and reorganized into one clean
  folder. Nothing about the live website, the mobile app, or the database
  changed.
- **What's still open:** the password-security fix still needs a written
  plan before any code touches it; the list of smaller bugs found earlier
  still needs you to say which matter most; the 4 leftover launcher
  scripts still need a decision on where they permanently live.
- **Git:** everything is staged, nothing is committed — say the word
  when you want it committed.

## Next steps (still unchanged)
- Decide whether the 4 remaining `.bat` files should also move somewhere
  more permanent, or stay in the notes folder long-term.
- The security fix (REQ-SEC-001, password hashing) is still waiting on a
  written plan before any code gets touched.

## Later still, same day — a quick secrets question

You had `admin-panel\.env.local` open and asked whether the Supabase anon
key in it is private. It isn't — that key is meant to be public and ships
in the browser bundle either way; what actually protects the data is Row
Level Security on the tables, not hiding this key. You then said "ok" to
checking the file further, so I read it and confirmed two things: the real
secrets in that file (AWS keys, a Vercel token) are not the anon key and do
need to stay private, and the file itself is correctly kept out of git —
double-checked with `git check-ignore` (it's ignored) and `git log` on that
exact path (it has never been committed). Nothing was changed; this was
read-only verification.

## Files changed (this part)
None — read-only check.

## Next steps (still unchanged)
- Decide whether the 4 remaining `.bat` files should also move somewhere
  more permanent, or stay in the notes folder long-term.
- The security fix (REQ-SEC-001, password hashing) is still waiting on a
  written plan before any code gets touched.
- Optional, not yet requested: re-verify RLS is actually enabled/correct on
  the Supabase tables the admin panel queries — BOOTSTRAP already records
  RLS as disabled on several `mobile-app` tables; not re-checked this part.
