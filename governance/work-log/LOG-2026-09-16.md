# LOG-2026-09-16 — Teacher app Play Store closed testing: set up and submitted

## What I set out to do

Continue the Play Store publication for the Teacher App
(`com.satyamstars.teacher`). Coming into this session, the store listing
copy, screenshots, icon, feature graphic, privacy policy page, and a signed
release AAB already existed on disk from prior sessions, but nothing had
been confirmed as actually entered into Play Console.

## What I found

Using browser automation (Claude in Chrome) against the user's own
logged-in Play Console session, the app "Teacher App - Satyam School" already
existed in Draft status with 4 of 11 setup-checklist items done (privacy
policy, sign-in details, ads, category/contact). The store listing itself
turned out to already be fully filled in and just needed reviewing and
saving — app name, short/full description, icon, feature graphic, and 4
screenshots were all already uploaded from a prior session.

The teacher AAB at
`mobile-app/build/app/outputs/bundle/teacherRelease/app-teacher-release.aab`
was confirmed built *after* the RECORD_AUDIO/READ_PHONE_STATE manifest strip
(commit `2957274`) — verified by checking the merged-manifest output's
timestamp against the AAB's build timestamp and confirming the merged
manifest has no RECORD_AUDIO/READ_PHONE_STATE and `camera.any
required="false"`. So the existing AAB was safe to upload as-is, no rebuild
needed.

## What I did

Completed all 11 items on Play Console's "Provide app information and
create your store listing" checklist for the Teacher app:

- **Store listing**: reviewed and saved (was already complete); labeled the
  feature graphic as AI-assisted per the user's confirmation.
- **Content rating questionnaire**: answered as "All Other App Types", no
  ratings-relevant content, no user content sharing, no online
  media-content, not age-restricted, not primarily news/educational →
  came back **Everyone / All ages** across every region.
- **Target audience**: **18 and over** (staff-only app) — this skipped the
  child-directed app-details/ads/store-presence steps entirely.
- **Data safety**: filled in per `mobile-app/store-data-safety.md`'s draft —
  Name/User IDs/Phone number (Personal info, collected not shared,
  required, App functionality), Other in-app messages (help-desk, optional),
  Files and docs (question-bank/syllabus uploads, optional), App
  interactions (attendance/marks/homework, required). No data shared with
  third parties. Two judgment calls resolved with the user mid-session
  rather than guessed: the IARC "online content" question (answered No —
  administrative data isn't the media-content this question targets) and
  which store assets counted as AI-generated (feature graphic only).
- **Government apps**: No. **Financial features**: none. **Health**: none.
- **Advertising ID declaration**: No (blocked initial submission until
  answered — no ads/analytics SDK in the app).

Then set up and submitted closed testing:
- Used the existing "Closed testing - Alpha" track (already created,
  previously empty).
- Targeted **India** as the available country.
- Created a tester email list "Teacher App Testers" containing
  `sectofficework@gmail.com` (per user's choice — "just use my account for
  now" rather than gathering a full list first).
- Uploaded the signed AAB (copied to `D:\app-teacher-release.aab` at the
  user's request for easier drag-and-drop, since the original 106MB file
  was too large for the browser-automation upload tool's 10MB limit and the
  native file picker isn't something I can drive) — the user did the actual
  upload by hand.
- Created the release (version 1 / 1.0.0, 32.1MB delivery size), wrote
  release notes, and **submitted 13 changes for review** to Google after
  confirming with the user at each consequential step (AI-asset labeling,
  which content-rating answers to give, and the final submit).

## What I did NOT do

Did not gather a real list of 12+ testers — only one email is on the list,
matching the user's explicit choice this session. Did not touch the student
or attendance-kiosk apps' Play Store status at all. Did not apply for
production access — Play Console won't allow it yet (see below).

## Where things stand

All of Google's app-content/store-listing requirements for the Teacher app
are complete and the release is **in review** (Google's own estimate: within
7 days). This is not yet a path to production, though — Play Console's
Dashboard shows the actual gate:

- Needs **≥12 testers opted in** to the closed test — currently **0 opted
  in** (the one listed email hasn't accepted/installed anything yet).
- Needs the closed test to **run ≥14 days** with those testers active
  before "Apply for production" unlocks.

Logged as a tracked pending item in `governance/planning/TODO.md` (Backlog
section) with the same detail.

## Next steps

1. Wait for Google's review of the current submission (passive).
2. Gather ≥12 tester Gmail addresses from school staff, add them to the
   "Teacher App Testers" list in Play Console → Test and release → Closed
   testing → Testers.
3. Share the opt-in link (Play Console generates one once the release is
   live) and get those testers to actually install and open the app.
4. Let the closed test run 14 days with testers active, then come back to
   apply for production access.
