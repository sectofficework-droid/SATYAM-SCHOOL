# LOG-2026-09-22 — REQ-BUG-018 distribution blocker resolved; Cat3 Group A re-locked

## What I set out to do

You said "playstore work" to resume from a prior session. I checked Play
Console live rather than trusting memory, and found the picture had moved
since the last recorded status: a production incident (REQ-BUG-018) had
happened on 2026-09-21, one day after that prior session's snapshot, and
was only recorded in `TODO.md` — no work-log entry existed for it.

## What I found

REQ-SEC-002 Category 3 Group A's DB migration (`SUPABASE_CAT3_GROUP_A_RPCS.sql`,
applied 2026-09-19) had gone live before its matching Dart/session-token
code ever reached a real device. Every installed Teacher/Student app kept
calling those 9 tables directly as `anon` and got permission-denied errors
with no error handling — teachers reported "can't take attendance." A
fixed build (v1.0.0+3) was prepared same day but couldn't be distributed:
no working S3 upload path, and the Play Publisher API failed both at the
network layer (Teacher AAB) and on a missing permission (Student app,
403). The DB side was rolled back instead
(`SUPABASE_ROLLBACK_CAT3_GROUP_A_PENDING_APK.sql`) to stop the bleeding —
reopening the `anon` exposure Group A was meant to close.

I verified this live against Supabase (not just the TODO.md text) before
acting on it: all 9 tables did show RLS disabled + full `anon` grants,
confirming the rollback was still in effect.

## What I did

**1. Resolved the distribution blocker via manual upload.** You connected
Claude in Chrome and selected the two emergency AABs yourself through the
native file picker (~104-113MB each, too large for the browser-automation
upload tool). I copied both from
`mobile-app/build/app/outputs/bundle/{teacherRelease,studentRelease}/` to
`D:\Project\SSIS SCHOOL\` for easy access, then drove the rest of the Play
Console flow (create release, upload, release notes, preview/confirm).

- Teacher app: release 3 (1.0.0, versionCode 3) created on the Alpha
  closed-testing track, submitted for Google review. Was stuck at
  versionCode 2 (Sep 19) before this.
- Student app: release 2 (1.0.0, versionCode 3), submitted for Google
  review. Was stuck at versionCode 1 (Sep 17, the original submission)
  before this. You asked why Student needed a release with "no changes" —
  I checked `git diff --cached` rather than assuming, and confirmed
  Student *does* carry real changes: `student_home.dart`/
  `student_query_page.dart` (the same guard-pattern stuck-loading fix
  applied across ~10 pages), plus the shared `supabase_service.dart`
  (285 lines — the Cat3 session-token RPC wiring) and `auth_service.dart`.
  Student's Alerts/Queries screens hit the identical bug as Teacher's Mark
  Attendance.
- Confirmed with you via AskUserQuestion before each final "send for
  review" click — a real rollout action, same practice as prior Play
  Store sessions.
- The Play Publisher API's 403 on the Student app package was **not**
  fixed — this session's success was entirely the manual browser-upload
  path. A future session automating Student app uploads via API still
  needs that access grant added in Play Console Users & permissions.

**2. Re-locked Cat3 Group A's database restriction — at your explicit
direction, before adoption was confirmed.** I flagged the tradeoff first
(review hadn't cleared, zero devices had versionCode 3 yet, `app_versions`
had no populated rows so there's no force-update signal) and asked via
AskUserQuestion. You chose to apply it anyway, accepting that every
currently-installed device breaks again until it updates. Applied
migration `req_sec_002_cat3_group_a_relock_after_v3_submission` — since
the 2026-09-21 rollback had only ever disabled RLS and re-granted `anon`
(never dropped the RLS policies or RPC functions from the original
migration), this only needed to re-enable RLS and revoke `anon` grants,
not recreate anything. Live-verified immediately after: all 9 tables show
`rls_enabled: true`, `anon_grants: null`.

## What's left, on purpose

This is a **known, accepted, ongoing outage** starting now, not a bug to
silently roll back again: Mark Attendance, Leave Requests, Alerts,
Queries, Task Assignees, Daily Task Completions, Syllabus/Attendance Edit
Requests, and Teacher Documents will fail on every device still running
versionCode 1/2 until each updates. Do not re-run
`SUPABASE_ROLLBACK_CAT3_GROUP_A_PENDING_APK.sql` without you asking again
— that choice was already made explicitly this session.

Real next step once Google's review clears: populate `app_versions` rows
for `teacher`/`student` (the `app` column exists per the 2026-09-21
session's fix, but no rows were ever added) with `force_update`
considered, so the app itself can prompt users rather than relying on
passive Play Store auto-update. Not done this session — flagged, not
forgotten.

Group B (6 tables) and Group C (2 tables) of Cat3 remain not started, as
before — now additionally gated on not repeating this same
migration-before-distribution mistake.

## Files touched

- No application code changed this session (Play Store console work +
  one direct-to-production SQL migration only).
- `mobile-app/app-teacher-release.aab` / `app-student-release.aab` (built
  prior session, uploaded to Play Console this session).
- `D:\Project\SSIS SCHOOL\app-teacher-release.aab` /
  `app-student-release.aab` (copies made this session for upload
  convenience).
- Database: migration `req_sec_002_cat3_group_a_relock_after_v3_submission`
  applied directly to production via Supabase MCP.
- `governance/BOOTSTRAP.md`, `governance/planning/TODO.md` (this record).

Full technical detail: `planning/TODO.md` REQ-BUG-018,
`planning/REQ-SEC-002-CATEGORY3-RPC-PLAN.md`.
